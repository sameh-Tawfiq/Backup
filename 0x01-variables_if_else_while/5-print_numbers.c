#include <stdio.h>
/**
 * main - Entry point
 * Code prints the alphabet in lower case
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%i", num);
	}
	printf("\n");
	return (0);
}
